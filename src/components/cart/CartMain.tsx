import CartContent from "./CartContent"
import StripCart from "./StripCart"

const CartMain = () => {
  return (
    <div>
        <StripCart/>
        <CartContent/>
    </div>
  )
}

export default CartMain